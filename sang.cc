#include <stdio.h>
int main()
{
    int n,flag=0;
    printf("Enter number to be checked: ");
    scanf("%d", &n);
    i=1;
    for(i<=n/2;i
    if(n!=0 || n!=1) {
    flag=1;
           if(n%i==0){
            flag=1;
            
        if(flag==0){
            printf("PRIME!!");
            
    }

    }
}
    return 0;
}